sudo: false
language: python
python:
  - "3.6"
  - "3.7-dev"

install:
  - pip install pipenv --upgrade
  - pipenv install --dev --skip-lock
  - pipenv shell

script:
  - ./main.py --config=config/test.ini

cache: pip
